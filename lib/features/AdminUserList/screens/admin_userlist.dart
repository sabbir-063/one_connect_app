import 'package:flutter/material.dart';

class AdminUserlistScreen extends StatelessWidget {
  const AdminUserlistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Admin User List page");
  }
}
